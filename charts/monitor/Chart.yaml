apiVersion: v2
name: monitor
description: "Helm Chart para instalar el monitor de relecturas"

# application | library
type: application

# This is the chart version. Semantic Versioning (https://semver.org/)
version: 1.0.1

# This is the version number of the application being deployed.
appVersion: "1.0.1"

dependencies:

